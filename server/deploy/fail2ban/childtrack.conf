# /etc/fail2ban/filter.d/childtrack.conf
# Matches failed device-ingest auth (HTTP 401) and login brute force on /api/login.
[Definition]
failregex = ^<HOST> .* "(POST|GET) /api/(ingest|login|sos|poll) HTTP/[\d.]+" 401
            ^<HOST> .* "POST /api/login HTTP/[\d.]+" 429
ignoreregex =
